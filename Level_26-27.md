markdown
# Bandit Уровень 26 → 27

## Задача
Получить пароль для уровня `bandit27`, используя SUID-бинарник `bandit27-do` и обходя ограничения оболочки (`more`/`vii`).

## Что я сделал
1. Вошёл на уровень 26 с паролем `s0773xxkk0MXfdqOfPRVr9L3jJBU0gCZ`.
2. При входе запустился `more`/`vii`, блокирующий обычный ввод.
3. Обнаружил, что в `/home/bandit26` есть SUID-бинарник `bandit27-do`, который выполняется с правами пользователя `bandit27`.
4. Использовал его для чтения пароля:
   ```bash
   ./bandit27-do cat /etc/bandit_pass/bandit27
Полученный пароль записал.

Команды, которые изучил
more, vii — программы-ловушки, ограничивающие доступ.

! в more / vii — выполнение команд.

SUID-бит (s в правах) — выполнение программы с правами владельца.

Пароль для следующего уровня (bandit27)
text
(upsNCc7vzaRDx6oZC6GiR6ERwe1MowGB)
