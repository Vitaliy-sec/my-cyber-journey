markdown
# Bandit Уровень 27 → 28

## Задача
Клонировать Git-репозиторий через SSH и найти пароль для уровня `bandit28`.

## Что я сделал
1. Вошёл на уровень 27 с паролем (полученным на предыдущем этапе).
2. Столкнулся с блокировкой подключений к `localhost` при попытке `git clone`.
3. Обошёл блокировку, используя внешний адрес и SSH-ключ от `bandit26` (или настройки `ssh-agent`).
4. Клонировал репозиторий:
   ```bash
   git clone ssh://bandit27-git@bandit.labs.overthewire.org:2220/home/bandit27-git/repo
В репозитории изучил историю коммитов (git log) и нашёл коммит с паролем.

Прочитал пароль для уровня bandit28.

Команды, которые изучил
git clone — клонирование репозитория через SSH.

git log — просмотр истории изменений.

Обход блокировки localhost через внешний адрес.

Пароль для следующего уровня (bandit28)
text
(Yz9IpL0sBcCeuG7m9uQFt8ZNpS4HZRcN)
