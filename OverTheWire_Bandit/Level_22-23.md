markdown
# Bandit Уровень 22 → 23

## Задача
Найти cron-задачу для пользователя `bandit23`, которая создаёт временный файл с паролем, и прочитать его.

## Что я сделал
1. Находился на уровне `bandit22`
2. Посмотрел `/etc/cron.d/cronjob_bandit23`
3. Изучил скрипт `/usr/bin/cronjob_bandit23.sh`:
   - Скрипт получает имя пользователя через `whoami`
   - Генерирует MD5-хеш от строки `"I am user $myname"`
   - Записывает пароль в `/tmp/$хеш`
4. Вычислил хеш для пользователя `bandit23`:
   ```bash
   echo "I am user bandit23" | md5sum | cut -d ' ' -f 1
Прочитал файл /tmp/8ca319486bfbbc3663ea0fbeb81326349

Команды, которые изучил
md5sum — вычисление MD5-хеша

cut -d ' ' -f 1 — извлечение первого поля из вывода

Анализ скриптов cron-задач

Пароль для следующего уровня (bandit23)
OZf11ioIjMVN551jX3CmStKLYqjk54Ga
