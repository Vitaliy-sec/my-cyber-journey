markdown
# Bandit Уровень 26 → 27

## Задача
Получить пароль для уровня `bandit27`, используя SUID-бинарник `bandit27-do` и обходя ограничения оболочки (`more`/`vii`).

---

## Что я сделал

### 1. Вход на уровень 26
```bash
ssh bandit26@bandit.labs.overthewire.org -p 2220
Пароль: s0773xxkk0MXfdqOfPRVr9L3jJBU0gCZ

2. Столкновение с ограничением
При входе автоматически запускался more (или vii), который открывал файл /home/bandit26/text.txt и блокировал обычный ввод команд.

3. Попытки обхода
Уменьшал окно терминала, чтобы more перешёл в интерактивный режим (--More--).

Пытался использовать ! для выполнения команд.

Пытался использовать v для запуска редактора vim.

4. Получение пароля через SUID-бинарник
Внутри more (в интерактивном режиме) выполнил:

bash
/home/bandit26/bandit27-do cat /etc/bandit_pass/bandit27
(или через ./bandit27-do после перехода в нужную папку)

Команды, которые изучил
more / vii — программы-ловушки, ограничивающие доступ

! в more — выполнение команд из просмотрщика

v в more — запуск редактора vim

SUID-бинарники — выполнение команд с правами владельца

Пароль для следующего уровня (bandit27)
text
(upsNCc7vzaRDx6oZC6GiR6ERwe1MowGB)
