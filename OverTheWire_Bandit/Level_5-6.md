# Bandit Уровень 5 → 6

## Задача
Найти файл, который:
- имеет размер 1033 байта
- не является исполняемым
- находится где-то в папке `inhere`

## Что я сделал
1. Подключился: `ssh bandit5@bandit.labs.overthewire.org -p 2220`
2. Ввёл пароль от уровня 5: `40QYVPkxZOOE005pTW81FB8j8lxXGUQW`
3. `ls` — увидел папку `inhere`
4. `cd inhere` — перешёл в папку
5. `find . -size 1033c` — нашёл все файлы размером 1033 байта
6. Проверил права найденных файлов через `ls -l`
7. Нашёл файл `./maybehere07/.file2` без `x` в правах (`-rw-r---`)
8. `cat ./maybehere07/.file2` — прочитал файл и получил пароль

## Команды, которые изучил
- `find . -size 1033c` — найти файлы размером 1033 байта
- `ls -l` — посмотреть права файла (наличие `x` = исполняемый)
- Права `-rw-r---` означают: файл можно читать, но нельзя запускать

## Пароль для следующего уровня (bandit6)
`HWasnPhtq9AVKe0dmk45nxy20cvUa6EG`
